# Bot Dashboard 启动脚本使用说明

## 📋 文件清单

本项目包含以下启动脚本：

| 文件名 | 适用系统 | 用途 |
|--------|---------|------|
| `start.bat` | Windows | 开发环境一键启动 |
| `start.sh` | Git Bash / Linux / Mac | 开发环境一键启动 |
| `start-prod.bat` | Windows | 生产环境一键启动 |
| `start-prod.sh` | Git Bash / Linux / Mac | 生产环境一键启动 |

## 🚀 快速开始

### Windows 用户

#### 方式1：双击运行（最简单）

1. 在文件资源管理器中找到 `start.bat`
2. 双击文件运行
3. 等待服务器启动
4. 浏览器访问 http://localhost:3002

#### 方式2：命令行运行

```bash
# 在项目根目录下
start.bat
```

### Git Bash / Linux / Mac 用户

```bash
# 在项目根目录下
./start.sh
```

如果提示权限不足：

```bash
chmod +x start.sh
./start.sh
```

## 🔧 脚本功能说明

### 自动检查

启动脚本会自动执行以下检查：

1. ✅ **Node.js 检查**
   - 检测是否安装 Node.js
   - 显示当前版本信息
   - 如未安装，提示下载地址

2. ✅ **npm 检查**
   - 检测 npm 是否可用
   - 显示版本信息

3. ✅ **依赖检查**
   - 检测 `node_modules` 目录是否存在
   - 如不存在，自动运行 `npm install`
   - 显示安装进度

4. ✅ **端口检查**
   - 检测端口 3002 是否被占用
   - 如被占用，询问是否终止占用进程
   - 自动清理端口（需确认）

### 自动启动

完成所有检查后，脚本会自动启动开发服务器。

## 🎯 使用场景

### 开发环境（start.bat / start.sh）

适用于日常开发：

- ✨ 热重载功能
- 🐛 详细的错误信息
- 🔄 快速迭代开发
- 📊 开发者工具集成

### 生产环境（start-prod.bat / start-prod.sh）

适用于部署运行：

- ⚡ 性能优化版本
- 📦 压缩后的代码
- 🚀 更快的加载速度
- 💰 更少的资源占用

## 📝 常见问题

### Q1: 端口被占用怎么办？

**现象：** 启动时提示 "端口 3002 已被占用"

**解决方案：**

1. 脚本会自动询问是否终止占用进程
2. 输入 `Y` 确认终止
3. 脚本会自动清理端口并继续启动

### Q2: Node.js 未安装怎么办？

**现象：** 提示 "未检测到 Node.js"

**解决方案：**

1. 访问 https://nodejs.org/
2. 下载并安装 LTS 版本
3. 重新运行启动脚本

### Q3: 依赖安装失败怎么办？

**现象：** `npm install` 报错

**解决方案：**

```bash
# 清理缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新运行启动脚本
./start.sh  # 或 start.bat
```

### Q4: 如何修改端口号？

如果想使用其他端口（比如 3000）：

1. 编辑 `package.json`：

```json
{
  "scripts": {
    "dev": "next dev -p 3000",
    "start": "next start -p 3000"
  }
}
```

2. 重新运行启动脚本

### Q5: 如何停止服务器？

在运行脚本的终端窗口中按 `Ctrl + C`

### Q6: Git Bash 下脚本无法执行？

**现象：** 提示 "Permission denied"

**解决方案：**

```bash
# 添加执行权限
chmod +x start.sh
chmod +x start-prod.sh

# 重新运行
./start.sh
```

## 🔍 脚本执行流程

```
开始
  ↓
检查 Node.js
  ↓
检查 npm
  ↓
检查 node_modules
  ↓ (如不存在)
运行 npm install
  ↓
检查端口 3002
  ↓ (如被占用)
询问是否终止进程 → [Y] → 终止进程
  ↓                 [N] → 退出
启动开发服务器
  ↓
显示访问地址
  ↓
等待用户操作 (Ctrl+C 停止)
```

## 💡 高级用法

### 后台运行（Linux/Mac）

```bash
# 后台运行开发服务器
nohup ./start.sh > server.log 2>&1 &

# 查看日志
tail -f server.log

# 查找进程
ps aux | grep "next dev"

# 停止服务器
kill -9 <进程ID>
```

### 使用 PM2 管理（推荐生产环境）

```bash
# 安装 PM2
npm install -g pm2

# 使用 PM2 启动
pm2 start "npm run start" --name bot-dashboard

# 查看状态
pm2 list

# 查看日志
pm2 logs bot-dashboard

# 停止
pm2 stop bot-dashboard

# 重启
pm2 restart bot-dashboard
```

## 📞 获取帮助

如果遇到问题：

1. 查看终端输出的错误信息
2. 检查是否满足运行要求（Node.js、npm）
3. 查看 [README.md](README.md) 获取更多信息
4. 检查 `package.json` 配置是否正确

## 🎉 成功启动

当看到以下信息时，表示启动成功：

```
========================================
   正在启动开发服务器...
========================================

[信息] 访问地址: http://localhost:3002
[提示] 按 Ctrl+C 可停止服务器

▲ Next.js 14.2.0
- Local:        http://localhost:3002

✓ Ready in 3.4s
```

现在可以在浏览器中访问 http://localhost:3002 开始使用 Bot Dashboard！
